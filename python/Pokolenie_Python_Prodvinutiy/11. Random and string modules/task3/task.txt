Напишите функцию generate_password(length), которая принимает длину пароля, с помощью модуля random генерирует случайный пароль и возвращает его. Пароль должен содержать только буквы английского алфавита a..z, A..Z (в нижнем и верхнем регистре).

Примечание 1. Приведенные ниже тесты – это лишь примеры ответа. Возможны и другие варианты генерации паролей.

Примечание 2. Вызывать функцию generate_password() не нужно, требуется только реализовать ее