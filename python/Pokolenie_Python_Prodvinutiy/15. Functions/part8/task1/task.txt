Напишите функцию func(), используя синтаксис анонимных функций, которая принимает целочисленный аргумент и возвращает значение True, 
если он делится без остатка на 19 или на 13, или False в противном случае.

Примечание. Вызывать функцию func() не нужно, требуется только реализовать ее.